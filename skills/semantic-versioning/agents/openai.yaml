interface:
  display_name: "Semantic Versioning"
  short_description: "Choose regular or release commits with SemVer evidence."
  default_prompt: "Use $semantic-versioning to decide whether these changes need a regular or release commit and recommend the version when needed."
