interface:
  display_name: "Ship Check"
  short_description: "Judge push, merge, deploy, or release readiness."
  default_prompt: "Use $ship-check to judge whether this change is ready for its intended shipping boundary."
