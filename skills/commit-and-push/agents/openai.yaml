interface:
  display_name: "Commit And Push"
  short_description: "Assess SemVer, then commit and push safely."
  default_prompt: "Use $commit-and-push to run the SemVer gate, prepare the right commit, and push the current changes."
