interface:
  display_name: "Commit And Push"
  short_description: "Commit and push safely with SemVer-aware release routing."
  default_prompt: "Use $commit-and-push to review, version when release intent requires it, commit, and push the current changes."
